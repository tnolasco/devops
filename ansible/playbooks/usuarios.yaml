---

- name: Criando usuarios no SO
  hosts: all
  tasks:
   - name: criando os usuarios no servidor
     user:
      name: "{{ item }}"
      shell: /bin/bash
     with_items:
      - vaca
      - boy
      - jorge
      - judith
